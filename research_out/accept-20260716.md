# 验收报告 2026-07-16

覆盖轮次: report-20260716（`factor-research` 相对 `main` 仅此一轮研究 commit）

## A 越界

**通过**

- `git diff main...factor-research --stat` 仅：
  - `research_out/ledger.jsonl`
  - `research_out/report-20260716.md`
  - `research_out/specs/{ema_alignment,net_income_qoq,revenue_cagr_3y}.json`
- 无 engine/research/risk/analyzer/data/ingest/config/cli/strategy/tests/pyproject 等白名单外路径。

## A2 阈值

**通过** — `git diff main...factor-research -- trendspec/research/ trendspec/engine/` 空。

## B 未来函数

**通过（N/A 新因子）**

- 本轮无新增 `trendspec/factors/**` 文件。
- 机械扫描：`shift(-` clean；`requests|urllib|random` 在 factors 下 clean。

## C 复现抽查

本轮 **0 个通过假设**，抽 1 个负结论：

| 假设 | 命令 | 报告值 | 重跑值 | 判定 |
|------|------|--------|--------|------|
| H2 fund_net_income_qoq | `research ic --spec-file research_out/specs/net_income_qoq.json --market cn --start 2010-01-01 --horizon 20` | RankIC 3866 期；IC均值=0.0193；IC标准差=0.0780；IR=0.2471；IC胜率=58.12% | 同上逐字一致 | **一致** |

未抽「通过」假设（无）。未对 H1 重跑 quantile（报告已记 qcut Panic；属已知评估故障，非本轮数字捏造）。

## D 账实

**通过**

- D1：ledger 中 `date=2026-07-16` 的 3 条 `manual_research`（fund_revenue_cagr_3y / fund_net_income_qoq / ema_alignment）与报告 H1–H3 一一对应；指标与结论一致。
- D2：无「入库」结论，无因子 commit 需核对。
- D3：`git status` 干净（验收写本文件前）；无失败因子残留代码。

## 结论

**建议并入 main**

依据：改动仅研究产物、无阈值/框架篡改、无新因子 lookahead 风险、负结论 IC 可复现、ledger 与报告一致。本轮 0 winners，合并仅归档研究记录，不引入策略代码。

并入（用户手动）：

```bash
git checkout main && git merge factor-research
```
